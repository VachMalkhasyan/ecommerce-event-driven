[project]
name = "inventory-service"
version = "0.1.0"
description = "Inventory management service for e-commerce"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
