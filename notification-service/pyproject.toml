[project]
name = "notification-service"
version = "0.1.0"
description = "Notification service for e-commerce"
dependencies = [
    "fastapi",
    "uvicorn",
    "sendgrid",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
